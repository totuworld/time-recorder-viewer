import { css } from 'glamor';

export const flexParentDiv = css({
  display: 'flex',
  flexDirection: 'column',
});

export const flexChildDiv = css({
  flex: 1
});

export const flexParentHorizontalDiv = css({
  display: 'flex',
});