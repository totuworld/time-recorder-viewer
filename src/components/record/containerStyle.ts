import { css } from 'glamor';

export const flexParentDiv = css({
  display: 'flex',
  flexDirection: 'column'
});

export const flexChildDiv = css({
  flex: 1
});

export const flexParentHorizontalDiv = css({
  display: 'flex'
});

export const floatButton = css({
  position: 'fixed',
  right: 0,
  bottom: 0,
  padding: 20
});

export const remoteInfo = css({ 'border-left-color': '#36A2EB' });
